"""oh-my-personal-best — standalone coaching app on the Claude Agent SDK.

Reuses the plugin's routing (CLAUDE.md), specialists (agents/*.md), and the
ompb_core toolkit, owning the orchestration loop so the plan-critic safety gate
is preserved outside Claude Code. Entry point: ``apps.coach.app:main``.
"""
