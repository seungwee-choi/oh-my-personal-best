---
description: "Create a periodized marathon plan from your goal"
---

# pb-plan

## Dispatch

1. Read the full skill instructions from the active OMPB plugin root: `skills/race-plan/SKILL.md`.
2. Follow that SKILL.md exactly, treating the user's goal as:

```text
$ARGUMENTS
```

If the file is not directly readable from the current working directory, locate it under the
active `CLAUDE_PLUGIN_ROOT` / `OMC_PLUGIN_ROOT`, package root, or installed plugin directory,
then continue.
